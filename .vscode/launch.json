{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run current test",
            "runtimeExecutable": "jest",
            "runtimeArgs": [
                "${file}",
            ],
            "outputCapture": "std",
        }
    ]
}